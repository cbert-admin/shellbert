#!/bin/bash
alias cl="clear"

# Shorten commands
alias bat="batcat"

# git related
alias pull="git pull"

alias ls="exa --icons --color=always"
alias la="exa -alhGF --sort=type --color=always --icons"